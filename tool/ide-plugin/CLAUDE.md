# Claude Code Context

This is an IntelliJ IDE plugin being built as part of a Final Year Project (FYP).

## Project Instructions

The initial project requirements and design specification live in [`INSTRUCTIONS.md`](./INSTRUCTIONS.md). Read this to understand what the plugin must do, the data model, and the core design principles.

## Project Plan

A detailed, stage-by-stage implementation plan will be created separately and saved as [`PLAN.md`](./PLAN.md). This is the working plan to track progress across sessions.

When starting a new conversation:
1. Read `INSTRUCTIONS.md` to understand the project requirements
2. Read `PLAN.md` (once it exists) to see the current stage and what has already been completed
3. Ask the user which stage or task to work on next if it isn't clear

Update `PLAN.md` as stages are completed — mark them done so progress is tracked across sessions. Append to the plan. Do not change existing text.
